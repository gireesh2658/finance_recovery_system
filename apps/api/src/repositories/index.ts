// Repositories layer — Prisma data access
// Repositories will be added in subsequent phases.
// Per backend-dev-guidelines: repositories encapsulate queries, handle transactions, expose intent-based methods.
